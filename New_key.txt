ssh-keygen -t rsa -b 4096 -C kschandan113@gmail.com = this command is used to create a new rsa-key.

eval "$(ssh-agent -s)" = 1. Start the agent

ssh-add ~/.ssh/id_rsa = 2. Add your private key to the agent

cat ~/.ssh/id_rsa.pub (then copy the content)

copy and paste the ssh key on github/gitlab

git remote set-url origin https://github.com/kschandan113/chandan-git.git  = this command is used to connect to local to remote github https.

git remote set-url origin git@github.com:kschandan113/chandan-git.git = this command is used to connect to local to remote github ssh.

ssh -T git@github.com = Make sure your SSH key is added to your GitHub account.

git push --set-upstream origin master = this command is used to push git to github master


git remote set-url gl-origin https://gitlab.com/kschandan113/tutedude-gitlab.git

chandantech@SIDT00227D48A:~/tutedude/06_vc_git/chandan$ git remote -v
gl-origin       https://gitlab.com/kschandan113/tutedude-gitlab.git (fetch)
gl-origin       https://gitlab.com/kschandan113/tutedude-gitlab.git (push)
origin  https://github.com/kschandan113/chandan-git.git (fetch)
origin  https://github.com/kschandan113/chandan-git.git (push)
chandantech@SIDT00227D48A:~/tutedude/06_vc_git/chandan$ git push gl-origin master







